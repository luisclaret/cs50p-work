# This program is going to replace each space in a string by ...

# Ask the user to input the string
string_to_format = input("Please enter the string: ")

# Print the formatted string
print(string_to_format.replace(" ", "..."))
